use std::collections::HashMap;

fn workspace_command(command: &str) {
    let mut command_map: HashMap<&str, &str> = HashMap::new();
    command_map.insert("create_alias_command", "create_alias_command");
}
